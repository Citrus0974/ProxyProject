{
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "Launch App",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/app",
            "cwd": "${workspaceFolder}"  // Ensures config.yml in root is found
        }
    ]
}